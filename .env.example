# Google OAuth (optional — auth features disabled without it)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
SESSION_SECRET=
OAUTH_STATE_SECRET=

# API Key for POST /api/digests and PUT /api/config
API_KEY=

# Server
DIGEST_PORT=8767

# Allowed origins (comma-separated, defaults to localhost)
ALLOWED_ORIGINS=localhost,127.0.0.1

# Lark webhook for feedback notifications (optional)
# Create at: Lark > Group > Settings > Bots > Add Bot > Custom Bot (Webhook)
# Format: https://open.larksuite.com/open-apis/bot/v2/hook/<token>
FEEDBACK_LARK_WEBHOOK=

# Feishu/Lark webhook for digest push notifications (optional)
# 飞书群机器人 Webhook，生成 Digest 后自动推送到群
# Format: https://open.feishu.cn/open-apis/bot/v2/hook/<token>
FEISHU_WEBHOOK=
FEISHU_SECRET=

# DeepSeek translation via SiliconFlow (optional — enables POST /api/translate)
# Get your key at: https://cloud.siliconflow.cn/
DEEPSEEK_API_KEY=

# HTTP proxy for fetch script (optional — enables Reddit/GitHub/etc. behind GFW)
# HTTP_PROXY=http://127.0.0.1:7890

# Max article age in hours — articles older than this are filtered out (default: 72)
# MAX_ARTICLE_AGE_HOURS=72

# RSSHub instance URL for Twitter/X feed scraping (recommended)
# Self-host: docker run -d -p 1200:1200 diygod/rsshub
# Docs: https://docs.rsshub.app/deploy/
RSSHUB_URL=

# Twitter/X Cookies（RSSHub 抓取 Twitter 必需）
# 获取方式：登录 x.com → 浏览器 DevTools → Application → Cookies
# 复制 auth_token 值（40位十六进制字符串）
# 如果使用 RSSHub，需在 RSSHub 的环境变量中配置 TWITTER_AUTH_TOKEN
TWITTER_AUTH_TOKEN=
# ct0 Cookie 值（可选，部分 RSSHub 版本需要）
TWITTER_CT0=